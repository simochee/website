commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit "$1"

pre-commit:
  commands:
    biome:
      run: |-
        npx biome check --apply-unsafe --no-errors-on-unmatched --files-ignore-unknown=true "$1"
        git update-index --again